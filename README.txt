
# Take the folder and structure the html in a templates folder with the following structure
/templates
    |
    |---html files
    |

# Take the css file and structure it in a static folder same directory as templates and create css sub folder with style.css in it
/static
    | --- css files
        
